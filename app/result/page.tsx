function ResultPage({}) {
  //   const router = useRouter();
  //   const prediction = router.query.prediction;

  return (
    <div>
      <h1>Prediction Result</h1>
      {/* <p>{prediction || "Loading prediction..."}</p> */}
    </div>
  );
}

export default ResultPage;
